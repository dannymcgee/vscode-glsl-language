/**
 * perform an atomic compare-exchange operation to a variable
 * 
 * @param mem The variable to use as the target of the operation. 
 * @param data The data to be compared and potentially exchanged with . 
 */
int atomicCompSwap( inout int mem, uint data )

/**
 * perform an atomic compare-exchange operation to a variable
 * 
 * @param mem The variable to use as the target of the operation. 
 * @param data The data to be compared and potentially exchanged with . 
 */
uint atomicCompSwap( inout uint mem, uint data )


/**
 * sample a varying at specified offset from the center of a pixel
 * 
 * @param interpolant Specifies the interpolant to be sampled at the specified offset. 
 * @param offset Specifies the offset from the center of the pixel at which to sample . 
 */
float interpolateAtOffset( float interpolant, vec2 offset )

/**
 * sample a varying at specified offset from the center of a pixel
 * 
 * @param interpolant Specifies the interpolant to be sampled at the specified offset. 
 * @param offset Specifies the offset from the center of the pixel at which to sample . 
 */
vec2 interpolateAtOffset( vec2 interpolant, vec2 offset )

/**
 * sample a varying at specified offset from the center of a pixel
 * 
 * @param interpolant Specifies the interpolant to be sampled at the specified offset. 
 * @param offset Specifies the offset from the center of the pixel at which to sample . 
 */
vec3 interpolateAtOffset( vec3 interpolant, vec2 offset )

/**
 * sample a varying at specified offset from the center of a pixel
 * 
 * @param interpolant Specifies the interpolant to be sampled at the specified offset. 
 * @param offset Specifies the offset from the center of the pixel at which to sample . 
 */
vec4 interpolateAtOffset( vec4 interpolant, vec2 offset )


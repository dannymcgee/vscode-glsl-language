/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
float mod( float x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
vec2 mod( vec2 x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
vec3 mod( vec3 x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
vec4 mod( vec4 x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
float mod( float x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
vec2 mod( vec2 x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
vec3 mod( vec3 x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
vec4 mod( vec4 x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
double mod( double x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
dvec2 mod( dvec2 x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
dvec3 mod( dvec3 x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
dvec4 mod( dvec4 x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
double mod( double x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
dvec2 mod( dvec2 x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
dvec3 mod( dvec3 x )

/**
 * compute value of one parameter modulo another
 * 
 * @param x Specify the value to evaluate. 
 */
dvec4 mod( dvec4 x )


/**
 * find the nearest integer to the parameter
 * 
 * @param x Specify the value to evaluate. 
 */
float round( float x ) {}

/**
 * find the nearest integer to the parameter
 * 
 * @param x Specify the value to evaluate. 
 */
vec2 round( vec2 x ) {}

/**
 * find the nearest integer to the parameter
 * 
 * @param x Specify the value to evaluate. 
 */
vec3 round( vec3 x ) {}

/**
 * find the nearest integer to the parameter
 * 
 * @param x Specify the value to evaluate. 
 */
vec4 round( vec4 x ) {}

/**
 * find the nearest integer to the parameter
 * 
 * @param x Specify the value to evaluate. 
 */
double round( double x ) {}

/**
 * find the nearest integer to the parameter
 * 
 * @param x Specify the value to evaluate. 
 */
dvec2 round( dvec2 x ) {}

/**
 * find the nearest integer to the parameter
 * 
 * @param x Specify the value to evaluate. 
 */
dvec3 round( dvec3 x ) {}

/**
 * find the nearest integer to the parameter
 * 
 * @param x Specify the value to evaluate. 
 */
dvec4 round( dvec4 x ) {}


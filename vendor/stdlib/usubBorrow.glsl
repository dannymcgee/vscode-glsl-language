/**
 * subtract unsigned integers and generate borrow
 * 
 * @param x Specifies the first vector to be used in the subtraction operation. 
 * @param y Specifies the second vector to be used in the subtraction operation. 
 * @param borrow Specifies the variable to receive the borrow output of the difference. 
 */
uint usubBorrow( uint x, uint y, out genUType borrow )

/**
 * subtract unsigned integers and generate borrow
 * 
 * @param x Specifies the first vector to be used in the subtraction operation. 
 * @param y Specifies the second vector to be used in the subtraction operation. 
 * @param borrow Specifies the variable to receive the borrow output of the difference. 
 */
uvec2 usubBorrow( uvec2 x, uvec2 y, out genUType borrow )

/**
 * subtract unsigned integers and generate borrow
 * 
 * @param x Specifies the first vector to be used in the subtraction operation. 
 * @param y Specifies the second vector to be used in the subtraction operation. 
 * @param borrow Specifies the variable to receive the borrow output of the difference. 
 */
uvec3 usubBorrow( uvec3 x, uvec3 y, out genUType borrow )

/**
 * subtract unsigned integers and generate borrow
 * 
 * @param x Specifies the first vector to be used in the subtraction operation. 
 * @param y Specifies the second vector to be used in the subtraction operation. 
 * @param borrow Specifies the variable to receive the borrow output of the difference. 
 */
uvec4 usubBorrow( uvec4 x, uvec4 y, out genUType borrow )

